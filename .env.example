# Optional override. If omitted, Shadow-Dex uses the RPC bundled with seismic-react's seismicTestnet config.
VITE_RPC_URL=

# Deployed Shadow-Dex pool contract. Leave empty until deployment.
VITE_SHADOW_POOL_ADDRESS=

# Test token addresses used by the first pool.
VITE_TOKEN0_ADDRESS=
VITE_TOKEN1_ADDRESS=
