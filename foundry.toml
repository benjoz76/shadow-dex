[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
script = "script"
test = "test"
cache_path = "cache"
solc_version = "0.8.13"
evm_version = "mercury"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
seismic_testnet = "${SEISMIC_RPC_URL}"
